spring:
  application:
    name: bike-service
  cloud:
   config:
    enabled: true
    uri: http://localhost:8081
  datasource:
    name: bike-service
    url: jdbc:mysql://127.0.0.1:3306/prueba?useSSL=false&serverTimezone=UTC
    username: root
    password: